# Fixes Applied V9

- Removed the large top Global filters block from the dashboard shell.
- Kept filters inside the pages that already have search/filter controls.
- Attendance History now uses its own franchise/user/date filters only.
- Attendance Approvals now uses its own franchise/status/user filters only.
- Leave now uses its own status filter and keeps the Leave Return Planner under the Leave tab.
- Payroll now has an inline employee/manager search field instead of depending on the removed global user filter.
- Added CSS guard to hide any stale global filter bar and improve local filter alignment.
