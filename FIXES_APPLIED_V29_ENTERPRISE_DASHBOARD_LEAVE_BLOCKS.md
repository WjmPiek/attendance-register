# V29 Enterprise dashboard + leave block cleanup

Applied changes:
- Smart suggestions and Latest notifications now stack as full-width enterprise sections.
- Overview alert blocks use full-width aligned rows so manager/status text fits correctly.
- Lower overview sections keep the same width as the Approved leave and return dates section.
- Leave return planner hides the top summary rows and keeps the red/green block timeline with user names.
- Timeline columns are widened for cleaner readability.
